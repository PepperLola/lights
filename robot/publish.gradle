apply plugin: 'maven-publish'

group = 'com.pigmice.piled'
def pubVersion = "2023.0.0"

java {
    withSourcesJar()
    withJavadocJar()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            version = pubVersion
            from components.java
        }
    }
}